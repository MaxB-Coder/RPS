<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>RPSSL</title>
    <link rel="stylesheet" href="/style.css" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
    />
  </head>

  <body>
    <div class="main">
      <h1 class="heading text-white text-center">
        <% if(result === "P1 Win") { %> <%= `${player1.name}'s
        ${player1.weapon}` %> beats <%= `${player2.name}'s ${player2.weapon}` %>
        <% } else if(result === "P2 Win") { %> <%= `${player2.name}'s
        ${player2.weapon}` %> beats <%= `${player1.name}'s ${player1.weapon}` %>
        <% } else { %> Draw! Both chose <%= player1.weapon %> <% } %>
      </h1>

      <h2 class="heading text-white text-center">
        Score: <%= player1.score %> to <%= player2.score %>
      </h2>

      <form class="text-center" action="/game" method="get">
        <input type="hidden" name="state" value="<%= state %>" />
        <input
          id="index-link"
          class="mt-2 h5"
          type="submit"
          value="Another one"
        />
      </form>
    </div>

    <footer class="container fixed-bottom text-center text-white pt-2">
      <p>
        &copy;
        <script>
          document.write(new Date().getFullYear());
        </script>
        Max Blaschek. All Rights Reserved.
      </p>
    </footer>
  </body>
</html>
